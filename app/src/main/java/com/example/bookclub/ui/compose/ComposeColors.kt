package com.example.bookclub.ui.compose

import androidx.compose.ui.graphics.Color

/**
 * Matches drawable/gradient_background.xml's startColor - used as a flat fill here since that
 * drawable has no endColor (renders as a fade-to-transparent gradient, which reads as an
 * unintentional visual quirk rather than a deliberate design choice).
 */
val GradientBackground = Color(0xFFB5EAD7)
