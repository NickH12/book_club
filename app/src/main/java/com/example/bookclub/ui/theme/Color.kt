package com.example.bookclub.ui.theme

import androidx.compose.ui.graphics.Color

// Mirrors res/values/colors.xml so default-styled Material3 components (buttons,
// dialogs, progress indicators, text field focus color, ...) match the app's teal/
// brown brand instead of Compose's stock baseline purple.
val BrandPrimary = Color(0xFF2E7D9A)
val BrandSecondary = Color(0xFFF4A261)
val BrandAccent = Color(0xFF8B5A3C)
val BrandError = Color(0xFFF44336)
val BrandSurface = Color(0xFFFFFFFF)
val BrandOnPrimary = Color(0xFFFFFFFF)
val BrandOnSurface = Color(0xFF1A1A1A)
